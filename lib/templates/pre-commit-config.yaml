repos:
  - repo: local
    hooks:
      - id: audit
        name: {{PLUGIN_NAME}} audit
        entry: uv run scripts/audit.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
